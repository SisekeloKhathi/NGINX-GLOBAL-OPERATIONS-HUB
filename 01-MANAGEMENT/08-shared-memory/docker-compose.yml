services:

  nginx:
    image: nginx:alpine
    container_name: nginx-shared-memory
    ports:
      - "8086:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - backend

  backend:
    build:
      context: ./backend
    container_name: nginx-shared-memory-backend
