services:

  nginx:
    image: nginx:alpine
    container_name: nginx-automation
    ports:
      - "8087:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - backend

  backend:
    build:
      context: ./backend
    container_name: nginx-automation-backend
