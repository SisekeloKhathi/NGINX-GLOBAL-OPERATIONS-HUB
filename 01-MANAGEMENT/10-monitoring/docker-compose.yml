services:

  nginx:
    image: nginx-lab-nginx:alpine
    container_name: nginx-monitoring
    ports:
      - "8088:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - backend
    networks:
      - lab-network

  backend:
    build:
      context: ./backend
    image: nginx-lab-monitoring-backend
    container_name: nginx-monitoring-backend
    networks:
      - lab-network

networks:

  lab-network:
    driver: bridge
