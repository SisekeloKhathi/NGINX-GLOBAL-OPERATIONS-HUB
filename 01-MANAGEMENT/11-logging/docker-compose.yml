services:

  nginx:
    image: nginx-lab-nginx:alpine
    container_name: nginx-logging
    ports:
      - "8089:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - backend
    networks:
      - lab-network

  backend:
    build:
      context: ./backend
    image: nginx-lab-logging-backend
    container_name: nginx-logging-backend
    networks:
      - lab-network

networks:

  lab-network:
    driver: bridge
