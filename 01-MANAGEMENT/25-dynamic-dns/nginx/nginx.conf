events {}

http {

    resolver 127.0.0.11 valid=10s;

    server {
        listen 80;

        location / {
            set $backend "backend1:8080";

            proxy_pass http://$backend;
        }
    }
}
