services:

  nginx:
    image: nginx:alpine
    container_name: nginx-rate-limiting
    ports:
      - "8115:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - backend

  backend:
    build: ./backend
    container_name: rate-backend
