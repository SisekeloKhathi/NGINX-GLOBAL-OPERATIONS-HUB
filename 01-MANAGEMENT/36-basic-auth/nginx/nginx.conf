events {}

http {

    upstream auth_backend {
        server backend:8080;
    }

    server {
        listen 80;

        location / {

            auth_basic "NGINX Lab 36";
            auth_basic_user_file /etc/nginx/.htpasswd;

            proxy_pass http://auth_backend;
        }
    }
}
