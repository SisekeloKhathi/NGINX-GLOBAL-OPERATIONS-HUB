events {}

http {

    server {
        listen 80;

        location / {
            return 200 "NGINX MONITORING LAB 38\n";
        }

        location /nginx_status {
            stub_status;
            allow all;
        }
    }
}
